<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingVertical="6dp"
    android:paddingHorizontal="8dp">

    <!-- Tên món + giá -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvFoodName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Cà phê đen"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="#3D3D3D"
            tools:ignore="HardcodedText" />

        <TextView
            android:id="@+id/tvFoodPrice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="50.000 đ"
            android:textSize="13sp"
            android:textColor="#A8742A"
            tools:ignore="HardcodedText" />
    </LinearLayout>

    <!-- Cụm số lượng: [-] [qty] [+] -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btnMinus"
            android:layout_width="40dp"
            android:layout_height="match_parent"
            android:text=""
            android:drawableTop="@drawable/ic_minus_white_24"
            android:gravity="center"
            android:drawablePadding="0dp"
            android:background="@drawable/bg_qty_left"
            android:backgroundTint="@null"
            app:backgroundTint="@null"
            android:minWidth="0dp"
            android:includeFontPadding="false" />

        <TextView
            android:id="@+id/tvQty"
            android:layout_width="40dp"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="0"
            android:textSize="15sp"
            android:textColor="#166534"
            android:background="@drawable/bg_qty_middle"
            android:includeFontPadding="false"
            tools:ignore="HardcodedText" />

        <Button
            android:id="@+id/btnPlus"
            android:layout_width="40dp"
            android:layout_height="match_parent"
            android:text=""
            android:drawableTop="@drawable/ic_plus_white_24"
            android:gravity="center"
            android:drawablePadding="0dp"
            android:background="@drawable/bg_qty_right"
            android:backgroundTint="@null"
            app:backgroundTint="@null"
            android:minWidth="0dp"
            android:includeFontPadding="false" />
    </LinearLayout>

</LinearLayout>
